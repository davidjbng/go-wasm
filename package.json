{
  "name": "go-wasm",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"trunk serve\" \"tailwindcss -i index.css -o tailwind.css -w\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.3.0",
    "tailwindcss": "^3.0.0-alpha.1"
  }
}
